-- Universal Script Hub - Fixed Version
-- This works with the updated key system that creates UI directly after validation

-- Load your UI library
local Lynk = loadstring(game:HttpGet("https://raw.githubusercontent.com/vvisuall/lynkUI/refs/heads/main/source"))()

-- Create window - the key system will handle everything else
local Window = Lynk:CreateWindow({
    Name = "Universal Script Hub",
    Title = "LYNK | No.1 Scripthub", 
    Subtitle = "Find any script for any game | Made by @x_vvw on Discord",
    Accent = Color3.fromRGB(88, 101, 242),
    DefaultKeybind = Enum.KeyCode.Insert
})

-- Updated comments to reflect the new direct UI creation approach
-- The key system now creates the main UI directly after validation
-- No Window:Show() calls needed - everything is handled automatically

print("🔑 Key system initialized - enter your key to access the script hub!")
print("🎮 Current Game:", game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name)
print("✨ UI will appear automatically after successful key validation!")
